{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "meson compile",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build",
            "options": {
                "cwd": "${workspaceRoot}/bin"
            }
        }
    ]
}